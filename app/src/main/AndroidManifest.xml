<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khanakhajana">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KhanaKhajana">
        <activity android:name=".activity2.CartActivity"></activity>
        <activity android:name=".activity2.DescriptionActivity" />
        <activity android:name=".activity2.ResetActivity" />
        <activity android:name=".activity2.ForgotActivity" />
        <activity android:name=".activity2.RegisterActivity" />
        <activity android:name=".activity2.LogInActivity" />
        <activity android:name=".activity2.MainActivity" />
        <activity
            android:name=".activity2.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>